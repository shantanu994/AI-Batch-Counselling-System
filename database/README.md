# Database Setup

1. Create schema and tables:

```sql
SOURCE schema.sql;
```

2. Seed sample data:

```sql
SOURCE seed.sql;
```

Default sample login password for seeded users is:

- `Password@123`

Connection settings should match `backend/.env`.
